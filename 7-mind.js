const num1 = 5;
const num2 = 10;

function addValues () {
    console.log(`the sum is : ${num1 + num2}`);
}

addValues();
//wen you import a module, you actually envoke it
//when the node exports it, it wraps it in the function.
